Запрос это строка в которую входят команды, имена таблиц, полей выражения и другие данные
Заканчивается точкой с запятой.
Команды пишуться заглавными буквами, названия полей, таблиц, данные...-строчными.
SELECT id, name, surname FROM user;
SELECT
INSERT
INSERT INTO название_таблицы VALUES(значение1, значение2, значение3...);
UPDATE
DELETE
DELETE FROM news WHERE date< DATE_SUB(CURDATE(), INTERVAL 5DAY); удалим все новости опубликованные раньше 5 дней назад.